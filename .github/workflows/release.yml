name: Release

on:
  push:
    tags:
      - "v*"

jobs:
  release-ubuntu:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v7

    - name: Setup Alpine
      uses: jirutka/setup-alpine@v1
      with:
        branch: v3.24

    - name: Install build dependencies
      run: apk add build-base go
      shell: alpine.sh --root {0}

    - name: Get code dependencies
      run: go get .
      shell: alpine.sh {0}

    - name: Get tag
      run: echo "RELEASE_VERSION=${GITHUB_REF#refs/*/}" >> $GITHUB_ENV

    - name: Build
      run: go build -o idRehash -ldflags="-s -w -X 'main.Version=$RELEASE_VERSION'" .
      shell: alpine.sh {0}

    - name: Upload binary
      uses: svenstaro/upload-release-action@v2
      with:
        repo_token: ${{ secrets.GITHUB_TOKEN }}
        file_glob: true
        file: "**/idRehash"
        tag: ${{ github.ref }}

  release-windows:
    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v7

    - name: Set up Go
      uses: actions/setup-go@v7
      with:
        go-version: '1.26.3'

    - name: Get dependencies
      run: go get .

    - name: Get tag
      run: echo "RELEASE_VERSION=${GITHUB_REF#refs/*/}" >> $GITHUB_ENV
      shell: bash

    - name: Build
      run: go build -o idRehash.exe -ldflags="-s -w -X 'main.Version=$RELEASE_VERSION'" .
      shell: bash

    - name: Upload binary
      uses: svenstaro/upload-release-action@v2
      with:
        repo_token: ${{ secrets.GITHUB_TOKEN }}
        file_glob: true
        file: "**/idRehash.exe"
        tag: ${{ github.ref }}
